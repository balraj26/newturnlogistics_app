# Loaded by `expo start` / `expo run:android` (dev mode). Android EMULATOR reaches the host
# machine at 10.0.2.2. For a physical device, override in .env.development.local (gitignored).
EXPO_PUBLIC_API_URL=http://10.0.2.2:8000
